import * as target from './postgres-subscription.repository.js';

describe('postgres-subscription.repository', () => {
  it('should load postgres-subscription.repository', () => {
    expect(target).toBeDefined();
  });
});
