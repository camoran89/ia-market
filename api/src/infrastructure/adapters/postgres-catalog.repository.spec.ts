import * as target from './postgres-catalog.repository.js';

describe('postgres-catalog.repository', () => {
  it('should load postgres-catalog.repository', () => {
    expect(target).toBeDefined();
  });
});
