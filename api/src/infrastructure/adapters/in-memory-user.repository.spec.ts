import * as target from './in-memory-user.repository.js';

describe('in-memory-user.repository', () => {
  it('should load in-memory-user.repository', () => {
    expect(target).toBeDefined();
  });
});
