import * as target from './postgres-user.repository.js';

describe('postgres-user.repository', () => {
  it('should load postgres-user.repository', () => {
    expect(target).toBeDefined();
  });
});
