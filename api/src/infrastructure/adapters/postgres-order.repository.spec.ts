import * as target from './postgres-order.repository.js';

describe('postgres-order.repository', () => {
  it('should load postgres-order.repository', () => {
    expect(target).toBeDefined();
  });
});
