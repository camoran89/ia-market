import * as target from './file-subscription.repository.js';

describe('file-subscription.repository', () => {
  it('should load file-subscription.repository', () => {
    expect(target).toBeDefined();
  });
});
