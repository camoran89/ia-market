import * as target from './in-memory-order.repository.js';

describe('in-memory-order.repository', () => {
  it('should load in-memory-order.repository', () => {
    expect(target).toBeDefined();
  });
});
