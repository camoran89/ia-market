import * as target from './seller-catalog-update.dto.js';

describe('seller-catalog-update.dto', () => {
  it('should load seller-catalog-update.dto', () => {
    expect(target).toBeDefined();
  });
});
