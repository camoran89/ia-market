import * as target from './receipt-validation-result.type.js';

describe('receipt-validation-result.type', () => {
  it('should load receipt-validation-result.type', () => {
    expect(target).toBeDefined();
  });
});
