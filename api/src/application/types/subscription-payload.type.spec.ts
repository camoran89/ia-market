import * as target from './subscription-payload.type.js';

describe('subscription-payload.type', () => {
  it('should load subscription-payload.type', () => {
    expect(target).toBeDefined();
  });
});
