import * as target from './subscription-result.type.js';

describe('subscription-result.type', () => {
  it('should load subscription-result.type', () => {
    expect(target).toBeDefined();
  });
});
