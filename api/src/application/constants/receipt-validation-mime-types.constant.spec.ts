import * as target from './receipt-validation-mime-types.constant.js';

describe('receipt-validation-mime-types.constant', () => {
  it('should load receipt-validation-mime-types.constant', () => {
    expect(target).toBeDefined();
  });
});
