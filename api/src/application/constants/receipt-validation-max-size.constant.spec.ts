import * as target from './receipt-validation-max-size.constant.js';

describe('receipt-validation-max-size.constant', () => {
  it('should load receipt-validation-max-size.constant', () => {
    expect(target).toBeDefined();
  });
});
