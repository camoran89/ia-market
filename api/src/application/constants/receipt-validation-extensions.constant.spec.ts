import * as target from './receipt-validation-extensions.constant.js';

describe('receipt-validation-extensions.constant', () => {
  it('should load receipt-validation-extensions.constant', () => {
    expect(target).toBeDefined();
  });
});
